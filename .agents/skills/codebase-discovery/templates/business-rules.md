# Business Rules

> **Last updated:** YYYY-MM-DD
> **Scope:** Rules enforced (or expected) by <system>
> **Mode:** full | code-only
> **Status:** accepted knowledge unless flagged — see ../_discovery/assumptions-register.md

<!-- The load-bearing rules a new joiner must know — grouped by area. NOT every conditional in
the code. Each rule: what it is, when it applies, exceptions, and why (if known). Keep
citations in the traceability index, not here. -->

## <Rule area, e.g. Orders>

- **<Rule name>** — <the rule, stated plainly>. Applies when <condition>. Exceptions:
  <who/what can override>. Rationale: <policy / regulation / technical — or [assumption] if
  the why is unknown>.

## <Rule area, e.g. Billing>

- **<Rule name>** — <...>

<!-- Use [assumption] where the rule is inferred from code but the intent/why isn't confirmed.
Use [outdated]/[contradicted] where an existing doc disagreed with the code. -->
